{% extends 'invites/base.html' %}

{% block title %}My Invites - Invitation System{% endblock %}

{% block content %}
<div class="d-flex justify-content-between align-items-center mb-4">
    <h1><i class="fas fa-list me-2"></i>My Invitation Links</h1>
    <a href="{% url 'create_invite' %}" class="btn btn-primary">
        <i class="fas fa-plus me-2"></i>Create New Invite
    </a>
</div>

<!-- Filters and Search -->
<div class="card mb-4">
    <div class="card-body">
        <form method="get" class="row g-3">
            <div class="col-md-4">
                <label for="status" class="form-label">Status</label>
                <select name="status" id="status" class="form-select">
                    <option value="">All Status</option>
                    <option value="active" {% if status_filter == 'active' %}selected{% endif %}>Active</option>
                    <option value="expired" {% if status_filter == 'expired' %}selected{% endif %}>Expired</option>
                    <option value="used" {% if status_filter == 'used' %}selected{% endif %}>Used</option>
                </select>
            </div>
            <div class="col-md-6">
                <label for="search" class="form-label">Search</label>
                <input type="text" name="search" id="search" class="form-control" 
                       placeholder="Search by description, audience, or token..." value="{{ search_query }}">
            </div>
            <div class="col-md-2 d-flex align-items-end">
                <button type="submit" class="btn btn-outline-primary w-100">
                    <i class="fas fa-search me-2"></i>Filter
                </button>
            </div>
        </form>
    </div>
</div>

<!-- Invites List -->
{% if page_obj %}
    <div class="card">
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table table-hover mb-0">
                    <thead>
                        <tr>
                            <th>Token</th>
                            <th>Description</th>
                            <th>Target Audience</th>
                            <th>Status</th>
                            <th>Usage</th>
                            <th>Created</th>
                            <th>Expires</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for invite in page_obj %}
                        <tr>
                            <td>
                                <code class="text-primary">{{ invite.token|slice:":8" }}...</code>
                            </td>
                            <td>
                                <div class="fw-medium">{{ invite.description|truncatechars:30|default:"No description" }}</div>
                            </td>
                            <td>
                                <small class="text-muted">{{ invite.target_audience|truncatechars:25|default:"Not specified" }}</small>
                            </td>
                            <td>
                                {% if invite.is_usable %}
                                    <span class="badge bg-success">Active</span>
                                {% elif invite.is_expired %}
                                    <span class="badge bg-danger">Expired</span>
                                {% elif invite.current_uses >= invite.max_uses %}
                                    <span class="badge bg-warning">Used Up</span>
                                {% else %}
                                    <span class="badge bg-secondary">Inactive</span>
                                {% endif %}
                            </td>
                            <td>
                                <div class="d-flex align-items-center">
                                    <div class="progress flex-grow-1 me-2" style="height: 8px;">
                                        <div class="progress-bar" role="progressbar" 
                                             style="width: {{ invite.usage_percentage }}%"
                                             aria-valuenow="{{ invite.current_uses }}" 
                                             aria-valuemin="0" 
                                             aria-valuemax="{{ invite.max_uses }}">
                                        </div>
                                    </div>
                                    <small class="text-muted">{{ invite.current_uses }}/{{ invite.max_uses }}</small>
                                </div>
                            </td>
                            <td>
                                <small class="text-muted">{{ invite.created_at|date:"M d, Y" }}</small>
                            </td>
                            <td>
                                <small class="text-muted {% if invite.is_expired %}text-danger{% endif %}">
                                    {{ invite.expires_at|date:"M d, Y" }}
                                </small>
                            </td>
                            <td>
                                <div class="btn-group btn-group-sm" role="group">
                                    <a href="{% url 'invite_details' token=invite.token %}" 
                                       class="btn btn-outline-primary" title="View Details">
                                        <i class="fas fa-eye"></i>
                                    </a>
                                    <button type="button" class="btn btn-outline-secondary" 
                                            onclick="copyToClipboard('{{ request.build_absolute_uri }}{% url 'register_with_invite' token=invite.token %}')"
                                            title="Copy Link">
                                        <i class="fas fa-copy"></i>
                                    </button>
                                </div>
                            </td>
                        </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    <!-- Pagination -->
    {% if page_obj.has_other_pages %}
    <nav aria-label="Pagination" class="mt-4">
        <ul class="pagination justify-content-center">
            {% if page_obj.has_previous %}
                <li class="page-item">
                    <a class="page-link" href="?page=1{% if status_filter %}&status={{ status_filter }}{% endif %}{% if search_query %}&search={{ search_query }}{% endif %}">First</a>
                </li>
                <li class="page-item">
                    <a class="page-link" href="?page={{ page_obj.previous_page_number }}{% if status_filter %}&status={{ status_filter }}{% endif %}{% if search_query %}&search={{ search_query }}{% endif %}">Previous</a>
                </li>
            {% endif %}

            <li class="page-item active">
                <span class="page-link">{{ page_obj.number }} of {{ page_obj.paginator.num_pages }}</span>
            </li>

            {% if page_obj.has_next %}
                <li class="page-item">
                    <a class="page-link" href="?page={{ page_obj.next_page_number }}{% if status_filter %}&status={{ status_filter }}{% endif %}{% if search_query %}&search={{ search_query }}{% endif %}">Next</a>
                </li>
                <li class="page-item">
                    <a class="page-link" href="?page={{ page_obj.paginator.num_pages }}{% if status_filter %}&status={{ status_filter }}{% endif %}{% if search_query %}&search={{ search_query }}{% endif %}">Last</a>
                </li>
            {% endif %}
        </ul>
    </nav>
    {% endif %}

{% else %}
    <!-- Empty State -->
    <div class="card">
        <div class="card-body text-center py-5">
            <i class="fas fa-inbox fa-4x text-muted mb-3"></i>
            <h4 class="text-muted">No invites found</h4>
            {% if status_filter or search_query %}
                <p class="text-muted">Try adjusting your filters or search terms.</p>
                <a href="{% url 'invite_list' %}" class="btn btn-outline-primary">Clear Filters</a>
            {% else %}
                <p class="text-muted">You haven't created any invitation links yet.</p>
                <a href="{% url 'create_invite' %}" class="btn btn-primary">Create Your First Invite</a>
            {% endif %}
        </div>
    </div>
{% endif %}
{% endblock %}

{% block extra_js %}
<script>
function copyToClipboard(text) {
    navigator.clipboard.writeText(text).then(function() {
        // Show success message
        const toast = document.createElement('div');
        toast.className = 'position-fixed top-0 end-0 p-3';
        toast.style.zIndex = '1050';
        toast.innerHTML = `
            <div class="alert alert-success alert-dismissible fade show" role="alert">
                <i class="fas fa-check me-2"></i>Invite link copied to clipboard!
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        `;
        document.body.appendChild(toast);
        
        // Auto-remove after 3 seconds
        setTimeout(() => {
            if (toast.parentNode) {
                toast.parentNode.removeChild(toast);
            }
        }, 3000);
    }).catch(function(err) {
        console.error('Could not copy text: ', err);
        alert('Failed to copy link. Please copy manually.');
    });
}
</script>
{% endblock %}